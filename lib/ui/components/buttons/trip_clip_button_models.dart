/// Filled brand blue — states follow TripClip light/dark specs.
enum TripClipButtonVariant {
  primary,
  /// Solid orange (same in light & dark).
  primaryAlternative,
  /// Outlined royal / bright blue.
  secondary,
  /// Text-only / ghost.
  tertiary,
  /// Filled destructive red.
  destructive,
}

enum TripClipButtonIconPlacement {
  none,
  leading,
  trailing,
  iconOnly,
}
